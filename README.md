# FAGUZA

App personal de planeacion financiera (PWA estatica).
Desplegada en Netlify (sitio vermillion-creponne-c502b1) con deploy continuo desde `main`.

Archivos pre-compilados: `index.html` + `assets/index-*.js` + manifest + iconos. Sin paso de build.
